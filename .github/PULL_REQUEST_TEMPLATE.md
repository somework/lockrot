**What and why.** What changes, and what it fixes or adds.

- [ ] Tests cover the change (`composer test`, `composer stan`, `composer cs` all pass)
- [ ] README updated if the change is visible to a user
- [ ] A line added to the `Unreleased` section of `CHANGELOG.md`
